import React from 'react'

const Gallery = () => {
    return (
        <>
            {/* Masonary Layout  */}
            {/* <div className='imageContainer columns-2 sm:columns-3 md:columns-4 lg:columns-5 gap-4 p-5'>
            {images.map((image) => (
              <div key={image.id} className="image mb-4 break-inside-avoid">
                <img src={image.urls.small} alt={image.alt_description} className="w-full rounded-lg shadow-md" />
              </div>
            ))}
          </div>  */}
        </>
    )
}

export default Gallery